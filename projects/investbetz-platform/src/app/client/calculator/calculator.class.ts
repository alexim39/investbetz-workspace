import { UserDashboardClass } from '../client.class';

export class CalculatorClass extends UserDashboardClass {

  constructor() {

    // Call parent class constructor
    super();

  }

  /* protected getPercentage(percentageOf: number): number {
    return percentageOf / 100;
  } */
}
